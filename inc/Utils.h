#ifndef UTILS_H
#define UTILS_H

#include <string>

using namespace std;

class Utils
{
public:

    static string normalizeWord(
        string word);

    static string toLowerCase(
        string text);
};

#endif